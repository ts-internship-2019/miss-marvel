@*@model IEnumerable<iWasHere.Domain.Models.DictionaryCountry>

    @{
        ViewData["Title"] = "Country";
    }

    <h1>Country</h1>

    <body>

        <div class="demo-section k-content">
            <div>

                <p>

                    @(Html.Kendo().TextBox()
                  .Name("countryTextBox")
                  .HtmlAttributes(new { style = "width: 20%", title = "Country Name" })
                    )

                    @(Html.Kendo().Button()
                            .Name("textButton")
                            .HtmlAttributes(new { type = "button" })
                            .Content("Filter")
                            .Events(ev => ev.Click("onClick"))
                    )

                </p>
            </div>
        </div>


        @(Html.Kendo().Grid<iWasHere.Domain.Models.DictionaryCountry>()
            .Name("grid")
            .Columns(columns =>
            {
                columns.Bound(c => c.CountryName).Width(400).Filterable(ftb => ftb.Cell(cell => cell.Operator("contains").SuggestionOperator(FilterType.Contains)));
                columns.Bound(c => c.CountryCode).Width(400);
                columns.Command(c =>
                {
                    c.Custom("EditRecord").Text("Edit");
                });
                 columns.Command(c =>
                {
                    c.Custom("DeleteRecord").Text("Delete");
                });
            })
            .HtmlAttributes(new { style = "height: 380px;" })
            .Scrollable()
            .Groupable()
            .Sortable()
            .Pageable(pageable => pageable
                .Refresh(true)
                .PageSizes(true)
                .ButtonCount(5))
            .DataSource(dataSource => dataSource
                .Ajax()
                .Read(read => read.Action("Country_Read", "Dictionary"))
            )
        )




        <script>
            function onClick(e) {
                $(e.event.target).closest(".k-button").attr("id");
                console.log("Apasat");
            }



        </script>


    </body>

    @*<p>
        <a asp-action="Create">Create New</a>
    </p>*@



@model IEnumerable<iWasHere.Domain.Models.DictionaryCountry>



@{
    ViewData["Title"] = "Country";
    Layout = "~/Views/Shared/_Layout.cshtml";
}



<!DOCTYPE html>



<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Country</title>
</head>

<h1>Country</h1>
<body>
    @*<p>
        <a asp-action="AddEditLandmarkType">Filters:</a>
    </p>*@
    <div class="demo-section k-content">
        <div>



            <p>



                @(Html.Kendo().TextBox()
              .Name("custom")
              .HtmlAttributes(new { style = "width: 30%", title = "custom", id = "txtFilter" })
                )



                @(Html.Kendo().Button()
                        .Name("btnSubmit")
                        .HtmlAttributes(new { type = "button" })
                        .Content("Filter")
                        .Events(ev => ev.Click("DisplayData"))
                )

                @(Html.Kendo().Button()
                       .Name("btnSubmit1")
                       .HtmlAttributes(new { type = "button" })
                       .Content("Delete Filter")
                       .Events(ev => ev.Click("DisplayData1"))
                )


            </p>
        </div>
    </div>
    @(Html.Kendo().Grid<iWasHere.Domain.Models.DictionaryCountry>()
          .Name("grid")
          .HtmlAttributes(new { style = "height: 500px", id = "grdCountry" })
          .Columns(columns =>
            {
                columns.Bound(p => p.CountryId).Hidden();
                columns.Bound(p => p.CountryName).Width(400);
                columns.Bound(p => p.CountryCode).Width(400);
                columns.Command(c =>
                        {
                c.Custom("EditRecord").Text("Edit").Click("dgChange");
            });
                   columns.Command(c =>
                        {
                c.Custom("DeleteRecord").Text("Delete").Click("dgChange");
            });
            })
          .Scrollable()
          .Groupable()
          .Sortable()
          .Filterable()
          .Pageable(pageable => pageable
            .Refresh(true)
            .PageSizes(true)
            .ButtonCount(5))
          .DataSource(dataSource => dataSource
              .Ajax()
              .Read(read => read.Action("GetCountry", "Dictionary")
              .Data("myFunction"))
              .Update(update => update.Action("GetCountry", "Grid"))
          )
    )





</body>
<script>



    function DisplayData() {
        $("#grdCountry").data("kendoGrid").dataSource.read()
        //$.ajax(
        //    {
        //        url: "/Dictionary/GetAjax",
        //        type: 'post',
        //        dataType: "json",
        //        data: { filter: input},
        //        success: function (result) {
        //            var dialog = $("#win").data("kendoWindow");
        //            $("#dataWin").html(result);
        //            dialog.open();
        //        }
        //    })
    }
        function DisplayData1() {
        $("#grdCountry").data("kendoGrid").dataSource.read()
    }


    function dgChange(e) {
        grid = $("#grdCountry").data("kendoGrid");
        var row = e.currentTarget.closest("tr").cells[0].outerText;



    }



    function myFunction() {
        var filter = $("#txtFilter").val();
        var obj = {};
        obj["lFilter"] = filter;
        return obj;
    }
</script>
</html>

